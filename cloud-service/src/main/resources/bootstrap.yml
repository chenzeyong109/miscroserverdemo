 spring:
    application:
      name: CLOUD-SERVICE
   cloud:
      config:
        discovery:
          enabled: true
          service-id: CONFIG-SERVER  # 注册中心的服务名
        profile: pro  # 指定配置文件的环境
        fail-fast: true


